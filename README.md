# Mini Workflow Engine — AI Engineering Internship


This project implements a small workflow engine with FastAPI. It supports nodes, edges, branching, and looping. Includes an example Code Review workflow (rule-based).


## How to run


1. Create virtualenv and install dependencies


```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

